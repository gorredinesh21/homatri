================================================================================
                           HOMAATRI — HISTORY LOG
================================================================================

--------------------------------------------------------------------------------
[SECTION 1: SUMMARY OF WORK DONE TILL NOW (V1 ARCHITECTURE & EXPERIMENTS)]
--------------------------------------------------------------------------------

1. INITIAL FOUNDATION & HUGGING FACE ROUTER MIGRATION:
   - Built a food ordering WhatsApp-native chatbot POC using FastAPI, SQLite / PostgreSQL.
   - Dealt with legacy Hugging Face serverless API endpoint decommissioning (`api-inference.huggingface.co`) by migrating to Hugging Face Inference Router (`router.huggingface.co`) using OpenAI-compatible payload schema.
   - Created deterministic fallback parsing rules for offline stability when LLM services were unavailable or rate-limited.

2. AGENT-FIRST & AWS BEDROCK ARCHITECTURE:
   - Inverted platform design: "The AI is the runtime; the backend is its tool belt."
   - Integrated AWS Bedrock (`ChatBedrockConverse`) with models: Llama 4 Scout (`us.meta.llama4-scout-17b-instruct-v1:0`), Qwen3-Next 80B, Amazon Nova Lite/Micro, and Claude Haiku 4.5.
   - Implemented PostgreSQL 16 + pgvector (384-dim normalized vector embeddings for trio shared memory & RAG).
   - Designed ~30 domain tools across Customer, Chef, and Driver roles.
   - Added 3-way Mediator Agent relay tools (`send_message_to_customer`, `notify_chef`, `notify_driver`) for cross-role communication.
   - Implemented strict guardrails:
     * Context Assembler (`app/services/context.py`): Injects system policy, live order state, last-N transcript, vector trio memory, and rolling summary into every LLM turn.
     * Policy Engine (`app/services/policy.py`): Enforces state machine lifecycle rules as tool pre-conditions.
     * System-Authored Financial Integrity: Itemized bills, exact price calculations, and payment links generated programmatically by backend services (LLM never guesses prices or payment URLs). Handling of mid-cooking item additions via incremental top-up payment links.
     * CoT Sanitization: Programmatically stripped `<think>` / `<thinking>` tags and broken markdown link placeholders.

3. GCP VERTEX AI & GEMINI 3.6 FLASH UPGRADE:
   - Multi-provider support (`bedrock`, `vertex`, `gemini`) in `app/services/llm.py`.
   - Upgraded default primary engine to Gemini 3.6 Flash on GCP Vertex AI Agent Platform with global location configuration.
   - Authored interactive architecture masterclass presentation site (`docs/gcp-presentation/index.html`).

4. MULTI-USER SIMULATOR & LOCATION-BASED KITCHEN DISCOVERY:
   - Built 3-phone Web Simulator UI (`templates/index.html`) with real-time SSE (`/api/stream`).
   - Added character switcher dropdowns for multi-actor simulation (Customers: Dinesh, Priya, Rahul; Chefs: Ramesh, Sunita, Maria; Drivers: Vikram, Suresh).
   - Added location-based discovery tools (`list_nearby_chefs`, `select_chef`) enabling customers to share coordinates and receive active kitchen menus nearby.

5. REAL-TIME MICROSECOND TELEMETRY TRACKER:
   - Built microsecond-level telemetry span tracker (`app/services/telemetry.py` & `/telemetry` router).
   - Developed interactive flow diagram dashboard (`templates/telemetry.html`) with raw prompt, SQL query, vector search, and tool execution inspection.

6. FAST MCP SERVER & REPO HARDENING:
   - Added FastMCP server (`app/mcp_server.py`) exposing Homaatri tools over Model Context Protocol.
   - Cleaned up unneeded archives/caches and updated Handover & Research docs.

--------------------------------------------------------------------------------
[SECTION 2: FRESH START — REBUILDING FROM SCRATCH]
--------------------------------------------------------------------------------

DATE INITIATED: 2026-07-25
BRANCH: from-scratch

STATUS:
- Created a clean, blank branch `from-scratch`.
- All previous code is preserved safely in git branches (`main`, `gcp-poc-upgrade`, `aws-bedrock`, `gcp-gemini`).
- The working environment on branch `from-scratch` is reset to a completely fresh state.
- Going forward, we will build Homaatri component by component, conducting experiments, running tests, and recording all findings and code updates in this `history.txt` file.

--------------------------------------------------------------------------------
[SECTION 3: LOG OF EXPERIMENTS & BUILD STEPS]
--------------------------------------------------------------------------------

[EXPERIMENT 001 - 2026-07-25]: LangChain GCP Vertex AI Integration Test
- Account Verified: errog2107@gmail.com (gcloud config profile [errog])
- Project ID: homatri-503308
- Virtual Environment: Rebuilt `.venv` with Python 3.11.15 (`/home/dinesh/.local/bin/python3.11`).
- Test Execution: Verified basic setup.

[EXPERIMENT 002 - 2026-07-25]: Upgrade to Gemini 3.6 Flash & Direct SDK Implementation (No LangChain)
- Model Discovery: Tested models on GCP Vertex AI (`gemini-3.6-flash`, `gemini-2.5-flash`, `gemini-2.0-flash-001`, `gemini-1.5-pro`). Verified `gemini-3.6-flash` is active, accessible, and fast on location `global`.
- Updated `llm.py`: Switched model to `gemini-3.6-flash` using `ChatVertexAI` via LangChain.
- Created `llm_direct.py`: Implemented a direct invocation script using Google's official `google-genai` SDK (`google.genai.Client(vertexai=True, project="homatri-503308", location="global")`) **WITHOUT LangChain**.
- Test Executions:
  * `.venv/bin/python3 llm.py` -> Successfully returned "Hello from Gemini 3.6 Flash via LangChain!".

--------------------------------------------------------------------------------
[SECTION 4: CORE BUSINESS SPECIFICATIONS & SYSTEM FLOW]
--------------------------------------------------------------------------------

BUSINESS MODEL & CORE REQUIREMENTS:
1. ENTITIES (3 Core Actors):
   - Customers
   - Home-Cook Chefs
   - Delivery Drivers

2. MEAL WINDOWS & STRICT CUTOFFS (No Impulse Ordering):
   - Services offered: Lunch and Dinner only.
   - Lunch Ordering Cutoff: Must be placed BEFORE 12:00 PM. No lunch orders accepted after 12:00 PM.
   - Dinner Ordering Cutoff: Must be placed BEFORE 7:00 PM. No dinner orders accepted after 7:00 PM.

3. WHATSAPP BUSINESS API INTERFACE:
   - Primary interface: WhatsApp chat.
   - Customer messages "Hi" -> AI assistant engages, collects item & preference details, and places the order into DB.

4. BATCH PROCESSING & ROUTE OPTIMIZATION:
   - At cutoff times (12:00 PM for lunch, 7:00 PM for dinner), chefs receive their aggregated order checklists.
   - Delivery Route Optimization: System collects chef kitchen coordinates (pickups) and customer coordinates (drop-offs), using AI/algorithms to generate efficient route paths for delivery drivers.

5. OPERATIONAL SIGNALS & HANDSHAKES:
   - When food is ready and packed, Chef sends signal (e.g. "Order X is ready").
   - System automatically notifies the assigned Driver that Order X is ready for pickup.

6. AI CUSTOMER CARE & LIVE LOGISTICS TRACKING:
   - AI acts as an interactive customer care agent (e.g. customer asks "Where is my driver?" -> AI queries driver location/order state and responds with live status).


--------------------------------------------------------------------------------
[SECTION 5: MULTI-AGENT ARCHITECTURE & DATA ACCESS SPECIFICATION]
--------------------------------------------------------------------------------

AGENT ROLES & TOPOLOGY:
1. Four Agents Total:
   - Master / Manager Agent (Central Supervisor / Orchestrator)
   - Customer Agent (Domain Specialist)
   - Chef Agent (Domain Specialist)
   - Delivery Driver Agent (Domain Specialist)

2. COMMUNICATION PATTERN (Hub-and-Spoke Mediator):
   - Subagents cannot communicate directly with each other.
   - All inter-role communications are relayed through the Master Agent.
   - Master Agent can talk directly to all three subagents.

3. DATABASE PERMISSION & WRITE ROUTING PROTOCOL:
   - READ ACCESS: Unrestricted across all 4 agents. All agents have global read-only access to all database tables (both same-domain and cross-domain).
   - WRITE ACCESS:
     * Subagents: Scoped strictly to their own domain tables (Customer Agent -> `customer_*`, Chef Agent -> `chef_*`, Driver Agent -> `driver_*`).
     * Master Agent: Global write access across all database tables.
   - CROSS-DOMAIN WRITE ROUTING:
     * If a Subagent requests a cross-domain write, it asks the Master Agent -> Master Agent delegates/sends the write request to the target domain Subagent -> Target Subagent performs the write in its own domain.
     * If a write is initiated by the Master Agent itself (e.g. system cutoffs, system orchestration), the Master Agent executes the write directly using its own global write access.


4. ARCHITECTURE DOCUMENTATION:
   - Authored comprehensive specification file `ai agent architecture.md` detailing topology, read/write matrices, delegation flows, and operational lifecycles.

--------------------------------------------------------------------------------
[SECTION 6: DATABASE TABLE SCHEMA & INVENTORY SPECIFICATION]
--------------------------------------------------------------------------------

COMPLETE 18-TABLE INVENTORY:
1. CUSTOMER DOMAIN (Customer Agent Writes):
   - `customer_profiles`: Contact, delivery address, default coordinates.
   - `customer_orders`: Order header, total pricing, meal window, status.
   - `customer_order_items`: Line items, quantities, special requests.
   - `customer_payments`: Payment transaction links & payment status.
   - `customer_chat_history`: Customer WhatsApp chat transcript.
   - `customer_reviews`: Ratings & feedback for chefs and drivers.

2. CHEF DOMAIN (Chef Agent Writes):
   - `chef_profiles`: Kitchen details, address, kitchen coordinates.
   - `chef_menu_items`: Dish catalog, pricing, meal availability.
   - `chef_daily_inventory`: Daily batch capacity & units sold.
   - `chef_order_readiness`: Meal prep & packing readiness signals.
   - `chef_chat_history`: Chef WhatsApp chat transcript.

3. DRIVER DOMAIN (Driver Agent Writes):
   - `driver_profiles`: Driver details, vehicle type, active status.
   - `driver_locations`: Real-time GPS location telemetry.
   - `driver_trip_status`: Active delivery trip status.
   - `driver_chat_history`: Driver WhatsApp chat transcript.

4. MASTER / SYSTEM DOMAIN (Master Agent Writes):
   - `system_meal_windows`: Batch cutoff manager (Lunch 12 PM / Dinner 7 PM).
   - `system_delivery_routes`: High-level TSP route assignments.
   - `system_delivery_stops`: Sequential stop-by-stop route itinerary.
   - `system_agent_logs`: Audit log for Master Agent relay calls & write delegations.
   - `system_outbound_queue`: Outbound WhatsApp message dispatch queue.


5. DATABASE SCHEMA DOCUMENTATION:
   - Authored `table_structure.md` listing all 18 tables grouped by domain ownership.

--------------------------------------------------------------------------------
[SECTION 7: LANGGRAPH HUMAN-IN-THE-LOOP (HITL) & AGENT RELAY PATTERN]
--------------------------------------------------------------------------------

KEY INSIGHT & ARCHITECTURAL BREAKTHROUGH:
1. RAW TRANSCRIPT PASSING ANTIPATTERN REJECTED:
   - Passing raw chat history (e.g. last 10 messages) creates massive token bloat, causes context loss when window shifts, and pollutes specialist prompt contexts with unrelated dialogue.

2. STRUCTURED INTENT RELAY & HITL FLOW:
   - Subagents extract clean JSON intent payloads (e.g. `{"order_id": "ord_104", "request": "No garlic"}`).
   - Relay Chain: Customer Agent -> Master Agent -> Target Subagent (Chef/Driver Agent).
   - LangGraph `interrupt()`: Target Subagent triggers `interrupt()`, saving checkpoint state to DB and pausing execution.
   - WhatsApp Notification: Human actor (Chef/Driver/Admin) receives WhatsApp notification.
   - Resume Execution: Human reply triggers webhook, loading DB checkpoint and resuming graph execution to confirm back up the chain.


3. DOCUMENTATION:
   - Authored `langgraph_hitl_relay.md` specifying the complete HITL relay architecture, sequence diagrams, and use cases.

--------------------------------------------------------------------------------
[SECTION 8: BACKEND CONCURRENCY, STATE ISOLATION & WORKSPACE REORGANIZATION]
--------------------------------------------------------------------------------

WORKSPACE REORGANIZATION:
1. All markdown documentation files moved into a dedicated directory `markdowns/`:
   - `markdowns/ai agent architecture.md`
   - `markdowns/table_structure.md`
   - `markdowns/langgraph_hitl_relay.md`
   - `markdowns/backend_concurrency_and_state.md`
2. RULE ENFORCED: All future markdown documentation files will be saved in `markdowns/`.

BACKEND EXECUTION & CONCURRENCY CONCEPTS VERIFIED:
1. Server Definition: Single 24/7 Python FastAPI application handling WhatsApp Business API webhook POST requests (`/webhook`).
2. Agent Definition: Python execution node wrapping system prompt, Gemini 3.6 Flash LLM invocation, DB tool calls, and state management.
3. Phone-Number Authentication: Immutable E.164 phone numbers used for instant database lookup (`customer_profiles` / `chef_profiles` / `driver_profiles`). Unregistered users routed to onboarding.
4. Single Shared Container: 1 stateless Docker container serving all concurrent users via asynchronous I/O (`asyncio`), saving state to PostgreSQL.
5. Concurrency & State Isolation: Asynchronous tasks run in parallel. LangGraph `thread_id` (phone number) ensures 100% state isolation between concurrent user turns.

6. Documentation: Authored `markdowns/backend_concurrency_and_state.md`.

--------------------------------------------------------------------------------
[SECTION 9: CUSTOMER AGENT MASTER SPECIFICATION]
--------------------------------------------------------------------------------

CUSTOMER AGENT SPECIFICATION COMPLETED:
1. Persona: Hospitality Concierge on WhatsApp (warm, respectful, efficient, English/Hinglish).
2. Features Included:
   - Distance-Sorted Chef Discovery (`view_chefs_sorted_by_distance` via Haversine math).
   - WhatsApp Location Pin Ingestion (`update_customer_location_from_whatsapp_pin`).
   - Programmatic Bill Generation & Incremental Mid-Cooking Top-Up Billing (`generate_topup_payment_link`).
   - Meal Window Cutoff Guardrails (12:00 PM Lunch / 7:00 PM Dinner).
   - Past Order History & Receipts (`get_customer_order_history`).
   - Post-Delivery Ratings & Reviews (`submit_review`).

3. Documentation: Authored `markdowns/customer_agent.md`.

--------------------------------------------------------------------------------
[SECTION 10: CHEF, DRIVER, & MASTER AGENTS MASTER SPECIFICATIONS]
--------------------------------------------------------------------------------

1. CHEF AGENT SPECIFICATION (`markdowns/chef_agent.md`):
   - Persona: Kitchen Operations Lead (direct, precise, time-conscious).
   - Core Rule: Onboarding & menu pricing managed strictly by Admin in DB (no self-registration or chat pricing changes).
   - Actions (10 Actions): `get_chef_profile`, `set_daily_batch_capacity`, `toggle_dish_stock_status`, `get_chef_inventory_status`, `get_chef_batch_checklist`, `get_batch_item_totals`, `mark_order_packed_ready`, `get_assigned_driver_for_pickup`, `handle_customer_dietary_request_approval` (HITL `interrupt()`), `notify_chef_driver_arrival`.

2. DELIVERY DRIVER AGENT SPECIFICATION (`markdowns/delivery_driver_agent.md`):
   - Persona: Logistics Navigator (concise, location-aware, fast-paced).
   - Core Rule: Admin handles driver onboarding in DB. Deliveries are made to Apartment Gates / Security Guards (no doorstep flat delivery).
   - Multi-Order Gate Grouping: Consolidates orders sharing the same apartment gate coordinates into a single gate stop.
   - Sequential Leg Navigation: Single GCP Route Optimization API call at cutoff (saved in `system_delivery_stops`). Driver Agent dispatches single-leg navigation links (`Stop N -> Stop N+1`) one by one on WhatsApp as driver messages "Reached".
   - Actions (9 Actions): `get_driver_profile`, `update_driver_location`, `get_assigned_route_itinerary`, `dispatch_next_leg_navigation_link`, `mark_driver_reached_stop`, `mark_orders_picked_up`, `mark_gate_delivery_completed`, `report_unlocatable_address_hitl`, `report_vehicle_delay_alert`.

3. MASTER AGENT SPECIFICATION (`markdowns/master_agent.md`):
   - Persona: System Supervisor & Central Orchestrator (disciplined, impartial, global write authority).
   - Dynamic Context Assembler & Scoped Tools: Python pre-fetches live DB state and injects operational variables into the System Prompt on every turn.
   - Cross-Entity Double-HITL Relay: Central bridge executing cross-domain delegation and relaying HITL prompts across Customer, Chef, and Driver agents.
   - Actions (13 Actions): `validate_meal_window_cutoff_clock`, `lock_meal_window_batch`, `orchestrate_gcp_route_optimization`, `relay_customer_dietary_request_to_chef`, `process_order_cancellation_request`, `relay_order_ready_signal_to_driver`, `relay_gate_delivery_completed_to_customer`, `relay_unlocatable_address_to_customer`, `relay_traffic_delay_alert_to_customers`, `process_payment_webhook_confirmation`, `execute_cross_domain_write_delegation`, `enqueue_and_dispatch_whatsapp_message`, `log_system_audit_event`.


4. MASTER DOCUMENTATION CREATED & COMMITTED:
   - `markdowns/customer_agent.md`
   - `markdowns/chef_agent.md`
   - `markdowns/delivery_driver_agent.md`
   - `markdowns/master_agent.md`

--------------------------------------------------------------------------------
[SECTION 11: GOLDEN DEFENSIVE AI CODING RULES & OBSERVABILITY STANDARD]
--------------------------------------------------------------------------------

THE 4 GOLDEN DEFENSIVE CODING RULES (MANDATORY FOR ALL HOMAATRI AI CODE):
1. Rule 1: Enforce `return_direct=True` or route directly to `END` for terminal actions to prevent infinite LLM retry loops.
2. Rule 2: Implement hard pre-condition assertions in Python tool functions before performing any DB writes.
3. Rule 3: Use strict Pydantic tool input schemas to validate LLM tool parameters at the schema boundary.
4. Rule 4: Clean state schema hygiene (`TypedDict`/Pydantic) to prevent variable overwrites across graph nodes.

TIMEOUTS & OBSERVABILITY STANDARD:
1. Hard Loop Kill: `recursion_limit = 10` on all LangGraph invocations.
2. HITL Checkpoint Expiry: 15-minute TTL on all `interrupt()` states in PostgreSQL with background expiry worker.
3. Tracing: LangSmith enabled via environment variables (`LANGCHAIN_TRACING_V2=true`).

4. Documentation: Authored `markdowns/agent_coding_rules_and_observability.md`.

--------------------------------------------------------------------------------
[SECTION 12: PENDING ARCHITECTURE ROADMAP]
--------------------------------------------------------------------------------

THE 5 PENDING ARCHITECTURAL MILESTONES (TO-DO ROADMAP BEFORE CODING):
1. Milestone 1: Database Column Schemas & SQL DDL Definitions (18 tables, data types, precision, FK constraints, indexes).
2. Milestone 2: Agent Tools Design & Pydantic Mapping (Grouping/splitting actions into `@tool` defs, Pydantic schemas, docstrings).
3. Milestone 3: LangGraph State Machine & Graph Structure (State `TypedDict`, nodes, conditional edges, checkpointer).
4. Milestone 4: WhatsApp Business API Webhook Gateway Engine (FastAPI endpoint, verification handshake, payload parser, outbound queue).
5. Milestone 5: Payment Gateway Webhook & Financial Verification Flow (Razorpay/Stripe webhook, HMAC signature verification, order status transitions).

6. Documentation: Authored `markdowns/todo_architecture_roadmap.md`.

--------------------------------------------------------------------------------
[SECTION 13: ACTION DEPENDENCY ANALYSIS & 4-TIER TOOL BUILD SEQUENCE]
--------------------------------------------------------------------------------

CROSS-AGENT ACTION DEPENDENCY ANALYSIS:
1. Producer vs. Consumer Mapping: Identified that Customer ordering tools (`Customer.view_chefs_sorted_by_distance`, `Customer.add_item_to_order`) cannot be tested or built without Chef menus & daily capacity limits existing in DB.
2. Master Route Optimizer Dependencies: Master GCP Route Solver (`Master.orchestrate_gcp_route_optimization`) requires confirmed customer orders to generate pickup/drop-off stop sequences.
3. Driver Fulfillment Dependencies: Driver Agent navigation (`Driver.get_assigned_route_itinerary`, `Driver.dispatch_next_leg_navigation_link`) relies directly on Master Route sequence output.

4-TIER LOGICAL TOOL BUILD ORDER:
- Tier 1 (Build 1st - Foundation): Chef Inventory Tools (`Chef.set_daily_batch_capacity`, `Chef.toggle_dish_stock_status`) & Master Cutoff Engine (`Master.validate_meal_window_cutoff_clock`).
- Tier 2 (Build 2nd - Ingestion): Customer Agent Tools (`Customer.register_customer_profile`, `Customer.view_chefs_sorted_by_distance`, `Customer.create_customer_order`, `Customer.generate_initial_payment_link`).
- Tier 3 (Build 3rd - Orchestration): Master GCP Route Solver (`Master.orchestrate_gcp_route_optimization`) & Chef Batch Checklists (`Chef.get_chef_batch_checklist`, `Chef.mark_order_packed_ready`).
- Tier 4 (Build 4th - Fulfillment): Driver Agent Tools (`Driver.get_assigned_route_itinerary`, `Driver.dispatch_next_leg_navigation_link`, `Driver.mark_gate_delivery_completed`) & Cross-Domain Relays.

DOCUMENTATION & VISUAL MAP CREATED:
- Interactive HTML Map: `docs/action_dependency_map.html`

- Written Documentation: `markdowns/action_dependencies_and_build_order.md`

--------------------------------------------------------------------------------
[SECTION 14: CHEF AGENT PRODUCTION TOOL SPECIFICATIONS (TIER 1 FOUNDATION)]
--------------------------------------------------------------------------------

CHEF AGENT TOOL DESIGN COMPLETED (9 PRODUCTION LLM TOOLS):
1. Tool Identifiers: Converted tool input lookups from internal `chef_id` to `chef_phone` for 100% natural WhatsApp alignment and zero LLM UUID hallucination.
2. Merged Tool: Merged Action 5 (`get_chef_batch_checklist`) and Action 6 (`get_batch_item_totals`) into 1 comprehensive tool `get_chef_batch_checklist_tool`.
3. Counter-Offer Protocol: Upgraded Tool 8 (`respond_to_custom_request_tool`) from binary YES/NO to a 3-way negotiation enum (`ACCEPTED`, `DECLINED`, `COUNTER_OFFER` + `counter_offer_text`).

LIST OF 9 CHEF TOOLS:
1. `get_chef_profile_tool`
2. `set_daily_dish_capacity_tool`
3. `toggle_dish_stock_tool`
4. `check_daily_inventory_status_tool`
5. `get_chef_batch_checklist_tool` (Merged Checklist + Totals)
6. `mark_order_packed_ready_tool`
7. `get_assigned_driver_info_tool`
8. `respond_to_custom_request_tool` (Counter-Offer Protocol)
9. `check_driver_arrival_status_tool`

DOCUMENTATION CREATED & COMMITTED:

- Updated Specification: `markdowns/chef_agent.md`

--------------------------------------------------------------------------------
[SECTION 15: CUSTOMER AGENT PRODUCTION TOOL SPECIFICATIONS (TIER 2 INGESTION)]
--------------------------------------------------------------------------------

CUSTOMER AGENT TOOL DESIGN COMPLETED (11 PRODUCTION LLM TOOLS):
1. Tool Identifiers: Converted tool input lookups from internal `customer_id` to `customer_phone` for 100% natural WhatsApp alignment and zero LLM UUID hallucination.
2. 2-Step Onboarding UX Refinement: Refined Tool 2 (`register_customer_profile_tool`) to accept text name & address first, and prompt user on WhatsApp to share location pin attachment for Tool 3 (`update_customer_location_pin_tool`).
3. Unified Payment Tool: Merged Action 8 (`generate_initial_payment_link`) and Action 9 (`add_extra_items_mid_order` / `generate_topup_payment_link`) into 1 unified tool `generate_payment_link_tool` (Python auto-detects initial bill vs top-up bill).
4. Modular Cart Building: Kept `initialize_customer_order_tool` (Tool 6) and `add_item_to_order_tool` (Tool 7) separate for multi-turn cart building.

LIST OF 11 CUSTOMER TOOLS:
1. `get_customer_profile_tool`
2. `register_customer_profile_tool` (2-Step text onboarding)
3. `update_customer_location_pin_tool` (WhatsApp location attachment pin)
4. `find_nearby_home_kitchens_tool` (Haversine distance ranking)
5. `view_chef_menu_tool`
6. `initialize_customer_order_tool` (Cutoff clock check & draft cart)
7. `add_item_to_order_tool` (Incremental cart building)
8. `generate_payment_link_tool` (Unified Initial + Top-Up Billing)
9. `get_active_order_status_tool`
10. `get_order_history_tool`
11. `submit_order_review_tool`

DOCUMENTATION CREATED & COMMITTED:

- Updated Specification: `markdowns/customer_agent.md`

--------------------------------------------------------------------------------
[SECTION 16: MASTER AGENT PRODUCTION TOOL SPECIFICATIONS (TIER 3 ORCHESTRATION)]
--------------------------------------------------------------------------------

MASTER AGENT TOOL DESIGN COMPLETED (12 PRODUCTION LLM TOOLS):
1. Atomic Cutoff & GCP Route Solver: Merged Action 2 (`lock_meal_window_batch`) and Action 3 (`orchestrate_gcp_route_optimization`) into 1 atomic tool `execute_cutoff_batch_and_route_optimization_tool` to guarantee batch lock and route generation execute together at 12 PM / 7 PM cutoff.
2. Cross-Entity Double-HITL Relays: Built dedicated relay tools for dietary requests, cancellations, ready signals, gate delivery alerts, unlocatable address pin requests, and traffic delay updates.
3. Financial Webhooks & Delegation: Built payment gateway webhook processor, write delegation tool, outbound WhatsApp message dispatcher, and system audit logger.

LIST OF 12 MASTER TOOLS:
1. `validate_meal_cutoff_clock_tool`
2. `execute_cutoff_batch_and_route_optimization_tool` (Atomic Cutoff Lock + GCP Route Solver)
3. `relay_dietary_request_to_chef_tool`
4. `process_order_cancellation_tool`
5. `relay_order_ready_to_driver_tool`
6. `relay_gate_delivery_completed_tool`
7. `relay_unlocatable_address_request_tool`
8. `relay_traffic_delay_alert_tool`
9. `process_payment_gateway_webhook_tool`
10. `delegate_cross_domain_write_tool`
11. `dispatch_whatsapp_outbound_message_tool`
12. `log_system_audit_event_tool`

DOCUMENTATION CREATED & COMMITTED:

- Updated Specification: `markdowns/master_agent.md`

--------------------------------------------------------------------------------
[SECTION 17: DELIVERY DRIVER AGENT PRODUCTION TOOL SPECIFICATIONS (TIER 4 FULFILLMENT)]
--------------------------------------------------------------------------------

DELIVERY DRIVER AGENT TOOL DESIGN COMPLETED (8 PRODUCTION LLM TOOLS):
1. Tool Identifiers: Converted tool input lookups from internal `driver_id` to `driver_phone` for 100% natural WhatsApp alignment and zero LLM UUID hallucination.
2. Practical Telemetry Decision: Removed continuous live GPS telemetry streaming (`update_driver_location`) as impractical for motorcycle drivers in traffic. Location is tracked naturally at key stop checkpoints (`mark_driver_reached_stop_tool`, `mark_orders_picked_up_tool`, `mark_gate_delivery_completed_tool`).
3. Single-Leg Navigation Links: Dispatches single-leg Google Maps navigation links (`Stop N -> Stop N+1`) one by one on WhatsApp as driver messages "Reached".

LIST OF 8 DRIVER TOOLS:
1. `get_driver_profile_tool`
2. `get_assigned_route_itinerary_tool`
3. `dispatch_next_leg_navigation_link_tool`
4. `mark_driver_reached_stop_tool`
5. `mark_orders_picked_up_tool`
6. `mark_gate_delivery_completed_tool`
7. `report_unlocatable_address_hitl_tool`
8. `report_vehicle_delay_alert_tool`

DOCUMENTATION CREATED & COMMITTED:

- Updated Specification: `markdowns/delivery_driver_agent.md`

--------------------------------------------------------------------------------
[SECTION 18: ATOMIC ORDER CREATION & INTER-AGENT TOOL PROTOCOL SPECIFICATIONS]
--------------------------------------------------------------------------------

1. ATOMIC 1-STEP ORDER CREATION REFINEMENT:
   - Refined Customer Tool 6 (`initialize_customer_order_tool`) to accept an optional initial `items` list payload.
   - Eliminates 2-turn order redundancy: Python checks cutoff clock, creates `customer_orders` header, and inserts `customer_order_items` in 1 single atomic DB transaction!
   - Tool 7 (`add_item_to_order_tool`) is retained for subsequent dish additions in later turns.

2. DOCUMENTATION & REPO SYNC:
   
- Updated Specification: `markdowns/customer_agent.md`

--------------------------------------------------------------------------------
[SECTION 19: INTER-AGENT TOOL LINKING & PROTOCOL BLUEPRINT (ALL 40 TOOLS)]
--------------------------------------------------------------------------------

COMPLETE INTER-AGENT TOOL LINKING & PROTOCOL SPECIFICATIONS COMPLETED:
1. Tool Linking Classification: Every tool across all 4 agents classified as `STANDALONE_INTERNAL`, `CROSS_AGENT_LINKED`, or `MASTER_CORE_ENGINE_SERVICE`.
2. Protocol Rule Blueprint: Specified Trigger Flow, State Data Payload Schema, Loop & Retry Boundaries, and Terminal Resolution Conditions for all 40 LLM tools.
3. Key Architectural Rules Enforced:
   - Counter-Offer Protocol (Chef Tool 8): Max 1 counter-offer negotiation turn (`ACCEPTED`, `DECLINED`, `COUNTER_OFFER` + dynamic dish/qty payload).
   - Atomic Cutoff Lock & GCP Route Solver (Master Tool 2): Merged atomic execution at 12 PM / 7 PM cutoff calling GCP Route Optimization API.
   - 2-Step Onboarding UX (Customer Tool 2 & 3): Text name/address first ➔ WhatsApp location pin attachment second.
   - Unified Billing (Customer Tool 8): Auto-detects initial bill vs mid-cooking top-up.
   - Stop-Based Driver Handshakes (Driver Tools 4-6): Replaced continuous telemetry with practical stop arrival & gate completion handshakes.


DOCUMENTATION UPDATED & COMMITTED:
- `markdowns/chef_agent.md` (9 Tools)
- `markdowns/customer_agent.md` (11 Tools)
- `markdowns/master_agent.md` (12 Tools)
- `markdowns/delivery_driver_agent.md` (8 Tools)

--------------------------------------------------------------------------------
[SECTION 20: LEFT-TO-RIGHT MERMAID EXECUTION FLOWCHARTS (`graph LR`)]
--------------------------------------------------------------------------------

VISUAL EXECUTION FLOWCHARTS ADDED:
1. Standardized `graph LR` Mermaid Flowchart diagrams embedded into all 4 agent master specification files for every cross-domain linked tool.
2. Traces exact node-to-node execution paths, payloads, API calls, and terminal resolution conditions visually across Chef, Customer, Master, and Driver domains.

DOCUMENTATION UPDATED & COMMITTED:
- `markdowns/chef_agent.md` (Embedded Mermaid Flowcharts)
- `markdowns/customer_agent.md` (Embedded Mermaid Flowcharts)
- `markdowns/master_agent.md` (Embedded Mermaid Flowcharts)

- `markdowns/delivery_driver_agent.md` (Embedded Mermaid Flowcharts)

--------------------------------------------------------------------------------
[SECTION 21: ALL 40 PRODUCTION AGENT LLM TOOLS PYTHON BLUEPRINT]
--------------------------------------------------------------------------------

PRODUCTION BLUEPRINT IMPLEMENTED & VERIFIED:
1. Production Blueprint Script: Created `all_40_agent_tools.py` formally declaring all 40 LLM Tools with `@tool` decorators, Pydantic input schemas, docstrings, and cross-domain handoff highlights inside tool functions.
2. Verified Zero-Error Execution: Verified script execution with `.venv/bin/python3 all_40_agent_tools.py` (0 errors, 100% clean compilation).
3. LangGraph Decoupling Proof: Created `demo_langgraph_agents.py` proving zero circular imports across agent nodes via central StateGraph routing.
4. Milestone 2 Completed: Marked Milestone 2 in `markdowns/todo_architecture_roadmap.md` as 100% COMPLETED.

FILES CREATED, UPDATED & COMMITTED:
- `all_40_agent_tools.py` (40 LLM Tools Specification Code)
- `demo_langgraph_agents.py` (LangGraph Multi-Agent Handoff Demo)
- `markdowns/todo_architecture_roadmap.md` (Updated Roadmap Status)
- `markdowns/chef_agent.md`, `customer_agent.md`, `master_agent.md`, `delivery_driver_agent.md`

--------------------------------------------------------------------------------
[SECTION 22: MILESTONE 1 KICKOFF — TABLE DESIGN METHOD & CHEF AGENT TABLES]
--------------------------------------------------------------------------------

DATE: 2026-07-30

ENTITY MODEL LOCKED (5 core entities in interaction order):
1. Homaatri/System (Master Agent actor-face + `system_*` domain) — foundation + cutoff orchestration.
2. Admin (human, no agent, `admin_*`) — onboards chefs/riders, sets menu prices.
3. Chef (Chef Agent + `chef_*`).
4. Customer (Customer Agent + `customer_*`).
5. Rider (Driver Agent + `driver_*`).
   Plus 3 EXTERNAL INTEGRATIONS (no entity tables; integration state lives inside `system_*`):
   Payment Gateway (webhook idempotency), WhatsApp/Meta (inbound dedup + outbound status), Maps/Route API (route-run logs).
   NOTE: "Master" and "System" are the actor-face and data-face of the ONE Homaatri entity (chose single-entity model).

TABLE DESIGN METHOD LOCKED (bottom-up query inventory, then merge):
- Go agent by agent. For each tool, list every query the tool ITSELF runs (reads + writes) in a fixed table:
  Q# | Tool | R/W | what | table | filter/key cols | index implied | on-list?
- Each query emits a table entry. Do all agents (overlaps expected), then dedupe/merge SAME-ENTITY tables (union columns).
  Merge trigger = same real-world entity (same PK meaning), NOT merely shared columns.
- Access-pattern-first is used as a COMPLETENESS CHECK; extra operational/future tables added on top.

BASE RULES (apply to ALL agents):
1. PHONE IS THE NATURAL KEY — tables key on normalized phone (`chef_phone`/`customer_phone`/`driver_phone`); no phone->id resolve.
   Transactional rows (orders/items/payments/routes) keep `VARCHAR(36)` surrogate ids.
2. WRITE INVARIANT — a subagent tool WRITES only its own domain tables; every cross-domain write is a HANDOFF to Master (note only).
3. READS ARE GLOBAL — a tool may READ any table; cross-domain reads stay on the list, finalized under the owner agent.
4. CROSS-AGENT CALLS = NOTES — another agent's queries are covered under that agent (mapping recorded).
5. `driver_locations` DROPPED — no live GPS; rider position inferred from route progress; arrival = `system_delivery_stops.actual_arrival`.
6. DATA TYPES — money `DECIMAL(10,2)`; lat `DECIMAL(10,8)`/lng `DECIMAL(11,8)`; timestamps `TIMESTAMPTZ`; FKs `ON DELETE RESTRICT`.

PHONE NORMALIZATION (at webhook ingress, Indian numbers only for now):
- Strip spaces; `+91`(13-char) -> last 10; 10-digit stays; else invalid.
- PENDING: add the `919876543210` (12-digit, no `+`) case that WhatsApp Cloud API actually sends.

CHEF AGENT PASS COMPLETED (9 tools -> 12 clean queries, 5 owned tables):
- Owned tables: chef_profiles (PK chef_phone), chef_menu_items, chef_daily_inventory, chef_order_readiness, chef_chat_history.
- New enum: readiness_status_enum (PREPARING, PACKED_READY).
- Cross-domain reads noted for owners: customer_orders, customer_order_items (Customer); system_delivery_stops,
  system_delivery_stop_orders, system_delivery_routes (System); driver_profiles (Rider).
- Handoffs -> Master: Tool 6 (order->PACKED + notify driver); Tool 8 (hitl resume + counter-offer relay -> Customer).
- Query count fell ~21 -> 12: removed ~7 redundant phone->id resolves (phone-as-key) + moved 2 cross-domain writes to Master handoffs. No work lost.

FILES CREATED / UPDATED:
- `markdowns/chef_tables.md` (NEW — full Chef table design & query access map)
- `markdowns/chef_agent.md` (appended Section 3: Table Design & Query Access Map)
- `history.txt` (this Section 22)

NEXT: Customer agent query pass (11 tools), same format and base rules.

--------------------------------------------------------------------------------
[SECTION 23: CUSTOMER AGENT TABLES — QUERY PASS & PAYMENT PROTOCOL]
--------------------------------------------------------------------------------

DATE: 2026-07-30

CUSTOMER AGENT PASS COMPLETED (11 -> 10 tools; 22 queries + 2 delegated executors; 6 owned tables).

TOOL CHANGE:
- Tool 3 (`update_customer_location_pin_tool`) FOLDED INTO `register` as a 2-phase HITL onboarding flow:
  write name/address (is_registered=false) -> interrupt() "share location" (if/else validates reply) -> save coords -> is_registered=true
  -> "thank you for registering, would you like to see kitchens near you?". So 11 -> 10 tools.

PAYMENT PROTOCOL (locked, double-state relay via Master):
- Customer computes FINAL AMOUNT -> hands amount to Master. Customer never touches the gateway.
- Master mints the UPI link via the external provider (Razorpay/etc.) -> returns link to Customer -> Customer wraps text + sends to user.
- TWO parallel LangGraph interrupts (states): Master waits on the provider WEBHOOK (`PAYMENT_AWAIT_PROVIDER`);
  Customer waits on Master's APPROVAL (`PAYMENT_AWAIT_MASTER_APPROVAL`).
- On webhook: Master verifies HMAC + dedups (`system_payment_webhook_events`) -> delegates customer_payments->PAID and
  customer_orders->CONFIRMED to the Customer executor -> approves the waiting Customer interrupt -> Customer notifies user.
- No new tables: reuses customer_payments (customer domain), system_payment_webhook_events + system_hitl_sessions (System).

DESIGN DECISIONS APPLIED:
- Cutoff check = DIRECT READ of system_meal_windows (no LLM hop). [flag A resolved]
- Payment gateway owned by Master; customer_payments records owned by customer domain (delegated executor). [flag B resolved]
- `units_sold` DERIVED = SUM(customer_order_items.quantity) for chef+dish+date -> NO cross-domain write on order creation. [flag C]
- SNAPSHOTS: kitchen_name on order; dish_name+unit_price+service_date on item (immutable; correct receipts, no cross-domain join). [flag D]
- Avg chef/driver ratings DERIVED from customer_reviews via global read -> no write to chef_/driver_profiles. [flag E]

DELEGATED-WRITE EXECUTORS (customer domain, triggered by Master) — where cross-domain writes INTO customer land:
- DW1 execute_order_status_transition -> customer_orders (BATCHED/PACKED/PICKED_UP/DELIVERED/CONFIRMED/CANCELLED)
- DW2 execute_payment_status_update -> customer_payments (PAID/FAILED/REFUNDED)

OWNED TABLES (6): customer_profiles (PK customer_phone), customer_orders, customer_order_items, customer_payments,
  customer_reviews, customer_chat_history.
NEW ENUMS: order_status_enum, payment_status_enum, payment_type_enum.
NEW system_hitl_sessions interrupt types introduced: AWAIT_LOCATION_PIN, PAYMENT_AWAIT_MASTER_APPROVAL, PAYMENT_AWAIT_PROVIDER.

CROSS-DOMAIN READS NOTED FOR OWNERS: chef_profiles/chef_menu_items/chef_daily_inventory/chef_order_readiness (Chef);
  system_meal_windows/system_settings/system_delivery_stops/system_delivery_routes/system_hitl_sessions/system_payment_webhook_events (System);
  driver_profiles (Rider).

FILES CREATED / UPDATED:
- `markdowns/customer_tables.md` (NEW — full Customer table design & query access map)
- `markdowns/customer_agent.md` (appended Section 3: Table Design & Query Access Map)
- `history.txt` (this Section 23)

NEXT: Rider (Driver) agent query pass (8 tools), same format and base rules.

--------------------------------------------------------------------------------
[SECTION 24: RIDER (DRIVER) AGENT TABLES — QUERY PASS]
--------------------------------------------------------------------------------

DATE: 2026-07-30

RIDER (DRIVER) AGENT PASS COMPLETED (8 tools; 7 on-list queries + 5 handoff notes; 3 owned tables).

OWNED TABLES (3; driver_locations DROPPED):
- driver_profiles (PK driver_phone; Admin-onboarded) — driver_name, vehicle_info, active_status, current_assigned_route_id.
- driver_trip_status (PK trip_id) — driver_phone, route_id, service_date, status (trip_status_enum), current_stop_index.
- driver_chat_history (written by runtime per turn).
NEW ENUM: trip_status_enum (ASSIGNED, EN_ROUTE_PICKUP, AT_KITCHEN, EN_ROUTE_DELIVERY, AT_GATE, COMPLETED).

KEY DECISIONS:
- KEEP driver_trip_status (not derived). It is an own-domain write (free) AND doubles as the cross-domain LIVE-TRACKING
  "progress list" board: Customer & Chef agents read `system_delivery_stops (ordered drop list) JOIN driver_trip_status
  (current_stop_index + completed)` to show the route, completed stops, current position, and pending stops.
  Replaces dropped GPS; upgrades to a map view later.
- The RIDER is the MOST Master-mediated agent: its only own-domain writes are trip-phase updates to driver_trip_status.
  All operational state (stop status, order status, ETAs) lives in System/Customer domains -> Master handoffs.

MASTER HANDOFF — TWO KINDS CLARIFIED (already implied by delegate rule):
- Target = SYSTEM domain (Master's own) -> Master writes DIRECTLY. e.g. stop->ARRIVED (T4), stop->COMPLETED (T6), ETA recalc (T8).
- Target = another SUBAGENT domain -> Master DELEGATES to that domain's executor. e.g. customer_orders->PICKED_UP (T5)/DELIVERED (T6) -> Customer DW1.

NEW system_hitl_sessions interrupt type: UNLOCATABLE_ADDRESS (waiting_on = CUSTOMER).

MASTER GAP SURFACED: Master needs a STOP-STATUS write mechanism (ARRIVED/COMPLETED/ETA recalc) — not in the current 12
Master tools. Add during the Master pass.

FILES CREATED / UPDATED:
- `markdowns/driver_tables.md` (NEW — full Rider table design & query access map)
- `markdowns/delivery_driver_agent.md` (appended Section 3: Table Design & Query Access Map)
- `history.txt` (this Section 24)

NEXT: Homaatri/System (Master) agent pass (12 tools) + Admin, then merge/dedupe all agent passes into the final schema.

--------------------------------------------------------------------------------
[SECTION 25: MASTER/SYSTEM AGENT TABLES + UNIFIED conversation_messages]
--------------------------------------------------------------------------------

DATE: 2026-07-30

MASTER/SYSTEM PASS COMPLETED (12 tools; ~25 on-list queries + delegations; 9 core system tables + observability + 1 runtime table).

RECONCILIATION (delegate-only supersedes spec): Master writes ONLY system_* directly and DELEGATES every subagent-domain
write to the owner executor (Customer DW1/DW2). Spec's "Master UPDATE customer_orders" lines are reclassified as delegations.
DELEGATIONS closing the loop: DW1 execute_order_status_transition <- BATCHED(cutoff)/CANCELLED(cancel)/PACKED(chef)/
DELIVERED(gate)/CONFIRMED(payment); DW2 execute_payment_status_update <- REFUNDED(cancel)/PAID(payment).

SYSTEM-OWNED TABLES (9 core): system_meal_windows (UNIQUE service_date+meal_type), system_settings (delivery_fee/cutoffs/tz),
system_delivery_routes, system_delivery_stops (UNIQUE route_id+stop_index), system_delivery_stop_orders (junction),
system_agent_logs (audit), system_outbound_queue (partial idx QUEUED + WA delivery status), system_hitl_sessions
(partial idx WAITING + 15-min TTL), system_payment_webhook_events (UNIQUE gateway_event_id idempotency).
OBSERVABILITY: system_route_optimization_runs.
NEW ENUMS: window_status_enum, route_status_enum, stop_status_enum, stop_type_enum, log_severity_enum, outbound_status_enum,
hitl_status_enum, hitl_interrupt_type_enum.
MASTER GAP FILLED: driver stop->ARRIVED handoff = Master DIRECT write to system_delivery_stops (system_* is Master's own).

UNIFIED CHAT HISTORY DECISION (conversation_messages):
- ONE unified, INSERT-ONLY (no update/delete) table `conversation_messages` stores EVERY message in both directions.
- Cols: message_id, phone, actor_role (CUSTOMER/CHEF/DRIVER), direction (INBOUND/OUTBOUND), source
  (USER/*_AGENT/MASTER_AGENT/SYSTEM), message_type, message_text, lat/lng, media_ref, related_order_id,
  wa_message_id (UNIQUE inbound dedup), raw_payload, created_at. Index (phone, created_at DESC); partition by month at scale.
- WRITTEN BY THE RUNTIME (messaging layer), NOT by agent tools: webhook logs inbound; dispatcher logs outbound
  (incl. Master's proactive notifications). This is a THIRD table category: RUNTIME/INFRASTRUCTURE (alongside domain
  tables and delegated executors). Global read.
- REPLACES the 3 per-domain *_chat_history tables AND system_inbound_messages (conversation_messages carries wa_message_id
  UNIQUE + raw_payload, serving inbound dedup too).
- CONTEXT ASSEMBLER: before each LLM call, fetch last 4-5 messages (both directions) for the phone; guardrail lets the LLM
  escalate to last-10 (hard cap) since users say "yes"/"okay" without context.

FILES CREATED / UPDATED:
- `markdowns/master_tables.md` (NEW — full Master/System table design)
- `markdowns/master_agent.md` (appended Section 3)
- `markdowns/chef_tables.md`, `customer_tables.md`, `driver_tables.md` + their agent .md files (dropped *_chat_history -> conversation_messages)
- `history.txt` (this Section 25)

NEXT: Admin entity pass, then MERGE/DEDUPE all agent passes into the final Milestone-1 schema (DDL).

--------------------------------------------------------------------------------
[SECTION 26: ADMIN ENTITY TABLES & ACCESS MODEL]
--------------------------------------------------------------------------------

DATE: 2026-07-30

ADMIN ENTITY PASS COMPLETED (back-office human operator via dashboard; 2 owned tables + 1 future; new access tier).

WHAT MAKES ADMIN DIFFERENT:
- NOT phone-based: dashboard email/password login -> surrogate admin_id + UNIQUE email (no phone natural key).
- NEW 4th ACCESS TIER (true superuser): Admin has GLOBAL READ + GLOBAL WRITE (direct to ALL tables). It bypasses the
  scoped-write/delegate model because the write-invariant exists to contain LLM mistakes, and Admin is a TRUSTED,
  AUTHENTICATED HUMAN. Accountability = AUDITING (every write logged to admin_activity_log), not delegation.

ACCESS LADDER (all actors):
- Subagent (Customer/Chef/Driver): global read; scoped write (own domain); cross-domain via Master delegate.
- Master: global read; own system_* write; delegates the rest.
- Admin (human): global read; GLOBAL WRITE (direct, all tables, audited).
- Admin AI agent (dashboard): global read; NO write (read-only).

ADMIN-OWNED TABLES (2 now + 1 future):
- admin_users (admin_id PK, email UNIQUE, name, password_hash, role admin_role_enum, is_active, last_login_at, timestamps).
- admin_activity_log (activity_id PK, admin_id FK, action, target_table, target_id, changes JSONB diff, created_at) — INSERT-only audit.
- admin_ai_queries (future/observability: admin_id, nl_question, generated_sql, row_count, latency_ms, status).
NEW ENUM: admin_role_enum (SUPER_ADMIN, OPS, SUPPORT).

KEY DECISIONS:
- admin_activity_log kept SEPARATE from system_agent_logs (distinct human-accountability audit domain).
- Mini dashboard AI agent is STRICTLY READ-ONLY (SELECT-only, read-only DB role / read replica). All writes go through
  dashboard UI forms with validation, never the free-form NL->SQL agent (avoids mass-DELETE/UPDATE footgun).
- Admin writes existing tables (chef_profiles, chef_menu_items, driver_profiles, system_settings) DIRECTLY — no new
  tables beyond admin_*. Matches the agent specs ("Admin handles onboarding & menu pricing in DB").


FILES CREATED / UPDATED:
- `markdowns/admin_tables.md` (NEW — Admin table design & access model)
- `history.txt` (this Section 26)

--------------------------------------------------------------------------------
[SECTION 27: MILESTONE 1 — TABLE COLUMN FINALIZATION & INTEGRITY GUARDS (ALL 24 TABLES)]
--------------------------------------------------------------------------------

DATE: 2026-07-31

100% PRODUCTION COLUMN SCHEMAS LOCKED (ALL 24 TABLES ACROSS 6 ENTITIES):
1. Chef Domain (4 Tables): `chef_profiles`, `chef_menu_items` (added `max_availability INT`), `chef_daily_inventory`, `chef_order_readiness` (made `box_count INT` nullable).
2. Customer Domain (5 Tables): `customer_profiles` (made `city` nullable), `customer_orders`, `customer_order_items`, `customer_payments`, `customer_reviews`.
3. Driver Domain (2 Tables): `driver_profiles`, `driver_trip_status`.
4. Master / System Domain (11 Tables across 4 Categories):
   - Category 1 (Batch & Cutoff Orchestration): `system_meal_windows`, `system_settings`, `system_route_optimization_runs`.
   - Category 2 (GCP Route Solver & Stop Dispatch): `system_delivery_routes`, `system_delivery_stops`, `system_delivery_stop_orders`.
   - Category 3 (System Resilience, HITL & Webhooks): `system_hitl_sessions`, `system_payment_webhook_events`, `system_agent_logs`.
   - Category 4 (Communications & Runtime Infrastructure): `system_outbound_queue`, `conversation_messages` (Unified Chat Log).
5. Admin Domain (2 Tables + 1 Future): `admin_users`, `admin_activity_log`, `admin_ai_queries`.

4 INTEGRITY IMPLEMENTATION GUARDS LOCKED:
- Guard 1: Atomic SQL Transactions (`db.transaction()`) with 100% all-or-nothing rollbacks.
- Guard 2: Hard Python Pre-Condition Assertions (`assert` guard clauses before writes).
- Guard 3: Centralized Delegated-Write Executors (Customer DW1 `execute_order_status_transition`, DW2 `execute_payment_status_update`).
- Guard 4: Automated `pytest` Integration Test Suite verifying all 40 LLM tools.


DOCUMENTATION UPDATED & COMMITTED:
- `markdowns/chef_tables.md` (Finalized 4 Chef tables)
- `markdowns/customer_tables.md` (Finalized 5 Customer tables)
- `markdowns/driver_tables.md` (Finalized 2 Driver tables)
- `markdowns/master_tables.md` (Finalized 11 Master/System tables)
- `markdowns/admin_tables.md` (Finalized 3 Admin tables)
- `HANDOVER.md` (Marked Milestone 1 as 100% COMPLETED)
- `history.txt` (this Section 27)

--------------------------------------------------------------------------------
[SECTION 28: PREFIXED UUID STANDARD, GUARD 2 MATRIX & 21 WRITE EXECUTORS]
--------------------------------------------------------------------------------

DATE: 2026-07-31

1. PREFIXED UUID PRIMARY KEY STANDARD LOCKED (ALL 24 TABLES):
   - Category A (3 Phone Natural Keys): `chef_profiles`, `customer_profiles`, `driver_profiles` (`VARCHAR(15)`).
   - Category B (21 Prefixed UUID Keys): 3-letter prefix + short UUID (`VARCHAR(36)`), e.g. `ord_`, `pay_`, `itm_`, `rt_`, `stp_`, `log_`, `out_`, `hitl_`, `evt_`, `run_`, `msg_`, `adm_`, `act_`, `aiq_`.

2. EXHAUSTIVE GUARD 2 ASSERTION MATRIX DEFINED:
   - Defined exact existence, ownership, cutoff, and state transition assertions across all 40 LLM tools.

3. MASTER WRITE EXECUTOR MAP (21 EXECUTORS):
   - Chef Domain: 3 Executors (`execute_daily_capacity_upsert`, `execute_dish_stock_toggle`, `execute_order_readiness_record`).
   - Customer Domain: 7 Executors (`execute_customer_registration_and_location`, `execute_customer_order_initialization`, `execute_add_item_to_order`, `execute_payment_record_creation`, `execute_submit_order_review`, DW1 `execute_order_status_transition`, DW2 `execute_payment_status_update`).
   - Driver Domain: 1 Executor (`execute_driver_trip_phase_update`).
   - Master Domain: 7 Executors (`execute_cutoff_batch_lock_and_routes_creation`, `execute_hitl_session_create_or_resume`, `execute_stop_status_update`, `execute_stop_eta_recalculation`, `execute_payment_webhook_idempotency_log`, `execute_outbound_whatsapp_enqueue`, `execute_system_audit_log`).
   - Shared Runtime: 1 Executor (`execute_conversation_message_insert`).
   - Admin Domain: 2 Executors (`execute_admin_profile_onboarding`, `execute_admin_activity_log`).

FILES CREATED / UPDATED:
- `markdowns/data_integrity_and_security_guards.md` (NEW — Full Integrity, Prefixed UUID & Executor Spec)
- `markdowns/chef_agent.md` (Appended Section 4)
- `markdowns/customer_agent.md` (Appended Section 4)
- `markdowns/master_agent.md` (Appended Section 4)
- `markdowns/delivery_driver_agent.md` (Appended Section 4)
- `history.txt` (this Section 28)



ALL 6 ENTITY PASSES DONE (Chef, Customer, Rider, Master/System, Admin + the runtime conversation_messages table).
NEXT: MERGE/DEDUPE all passes into the final Milestone-1 schema (CREATE TYPE enums + CREATE TABLE DDL).

--------------------------------------------------------------------------------
[SECTION 29: MILESTONE 3 DESIGN — LANGGRAPH STATE MACHINE & GRAPH]
--------------------------------------------------------------------------------

DATE: 2026-07-31

MILESTONE 3 (LangGraph runtime) DESIGNED — the "nervous system" wiring the 40 tools + 24 tables into a live,
durable, multi-agent conversation. Full spec: `markdowns/langgraph_state_machine.md`. Designed as 5 pieces:

PIECE 1 — STATE SCHEMA (`HomaatriGraphState`, 7 lean fields): messages (Annotated + add_messages reducer),
  sender_phone, current_role, target_node, event_payload, active_order_id, current_input (structured inbound
  {type,text,lat,lng,button_id}). PRINCIPLE: clipboard carries conversation + control-flow + identity + handoff ONLY;
  all domain data read FRESH from Postgres inside nodes each turn (no stale copies on the clipboard).

PIECE 2 — NODES (4): 3 specialist tool-loops (customer/chef/driver) + 1 master orchestrator. A node = fn(state)->state-update.
  INNER TOOL-LOOP = OPTION B (custom ~12-line ReAct loop) LOCKED — NOT create_react_agent. Reason: need control for
  Context Assembler, Guard-2 asserts, custom target_node/event_payload routing, interrupt() placement; uses only stable
  low-level primitives (bind_tools/.invoke/.tool_calls/ToolMessage) => low framework lock-in; latency dominated by LLM calls
  (loop mechanics negligible; B marginally leaner on checkpoints). Tool ISOLATION: each node binds ONLY its own agent's tools.
  Master node = orchestrator (routes handoffs, runs delegated executors, HITL, system events), not a chatty specialist.

  CONTEXT ASSEMBLER CONTRACT (every LLM call, user refinement): NEVER send bare current message. First fetch last 3-4
  INBOUND (user) + last 3-4 OUTBOUND (agent) from conversation_messages for the phone (~6-8, both sides), merge + sort by
  created_at, FORMAT WITH TIMESTAMPS + speaker (from direction/source), append current message, then send. Timestamps let
  LLM reason about time (cutoff proximity); terse "yes"/"okay" resolves against the window. Escalation: default 3-4 each,
  LLM may pull up to ~10 each if insufficient.

PIECE 3 — ROUTER (edges): pattern "nodes decide, router dispatches". Single central conditional edge `route_by_target`
  reads state.target_node -> next node or END. No node imports another node (zero circular imports). Safety: recursion_limit=10.

PIECE 4 — CHECKPOINTER: AsyncPostgresSaver (matches Postgres + async FastAPI; MemorySaver for tests). thread_id = normalized
  phone => per-user isolation + cross-turn memory + durability + HITL pause/resume. 3-WAY STATE OWNERSHIP (key decision):
  (a) LangGraph checkpointer = live control-flow for pause/resume, kept lean (message context rebuilt per turn, not
  accumulated); (b) conversation_messages = authoritative transcript + Context Assembler source; (c) system_hitl_sessions =
  business HITL ledger + 15-min TTL. 15-MIN TTL + EXPIRY WORKER: interrupt() can hang forever if human ghosts; on pause stamp
  expires_at + default_on_expiry; background worker polls WAITING+expired, resumes with safe default, marks EXPIRED (business
  logic the generic checkpointer can't do -> why the table exists separately).

PIECE 5 — ENTRY FLOW: 3 doors -> same graph. (A) WhatsApp inbound: normalize phone -> dedup+parse -> FRESH vs RESUME check
  (pending interrupt? e.g. location pin / chef YES / payment => resume paused graph) -> resolve identity in entry_node
  (chef_profiles? -> driver_profiles? -> customer_profiles? -> else UNKNOWN=new customer onboarding) -> run under
  thread_id=phone. (B) Cutoff cron 12PM/7PM -> master_node (batch+route). (C) Payment webhook -> master_node -> resumes
  customer PAYMENT_AWAIT_PROVIDER interrupt.

USER DECISIONS LOCKED THIS SESSION: Postgres (not SQLite); Option B custom node loop (keep LangGraph at edges, pin version);
  3-way state split; AsyncPostgresSaver; thread_id=phone; 15-min TTL worker; Context Assembler = both-sides timestamped window.

FILES CREATED / UPDATED:
- `markdowns/langgraph_state_machine.md` (NEW — full M3 design)
- `docs/history.txt` (this Section 29)

--------------------------------------------------------------------------------
[SECTION 30: POSTGRESQL 16 TRANSITION & 21 WRITE EXECUTORS IMPLEMENTATION (100% TEST PASS)]
--------------------------------------------------------------------------------

DATE: 2026-07-31

1. SQLITE PURGED & POSTGRESQL 16 NATIVE ENVIRONMENT LOCKED:
   - Removed `aiosqlite` dependency; enforced PostgreSQL via `asyncpg` / `psycopg`.
   - Updated `app/core/config.py` and `app/db/session.py` to connect natively to PostgreSQL:
     `postgresql+asyncpg://dinesh:homatri_pass@localhost:5432/homatri_db`.
   - Created `docker-compose.yml` and `.env`; started local PostgreSQL 16 container (`homatri_postgres`).
   - Executed `scripts/create_db.py` — ALL 24 TABLES SUCCESSFULLY CREATED IN LIVE POSTGRESQL 16!

2. ALL 21 WRITE EXECUTORS IMPLEMENTED & LOCKED (`app/executors/`):
   - Category 1 (Chef Domain - 3): `execute_daily_capacity_upsert`, `execute_dish_stock_toggle`, `execute_order_readiness_record`.
   - Category 2 (Customer Domain & Delegated DW1/DW2 - 7): `execute_customer_registration_and_location`,
     `execute_customer_order_initialization`, `execute_add_item_to_order`, `execute_payment_record_creation`,
     `execute_submit_order_review`, DW1 `execute_order_status_transition`, DW2 `execute_payment_status_update`.
   - Category 3 (Driver Domain - 3): `execute_driver_profile_upsert`, `execute_driver_trip_initialization`, `execute_driver_trip_phase_update`.
   - Category 4 (Master / System & Shared Runtime - 8): `execute_meal_window_lock_and_creation`,
     `execute_cutoff_batch_lock_and_routes_creation`, `execute_stop_status_update`, `execute_hitl_session_create_or_resume`,
     `execute_payment_webhook_idempotency_log`, `execute_outbound_whatsapp_enqueue`, `execute_conversation_message_insert`, `execute_system_audit_log`.

3. AUTOMATED INTEGRATION TEST SUITE (100% PASSING ON POSTGRESQL 16):
   - Created `tests/conftest.py` with `NullPool` asyncpg fixtures for clean event-loop test isolation.
   - Ran `pytest` suite — 16/16 tests passed cleanly in 26.53s (`test_chef_executors.py`, `test_customer_executors.py`,
     `test_driver_executors.py`, `test_master_executors.py`).

FILES CREATED / UPDATED:
- `markdowns/executors.md` (NEW — Master Specification for all 21 Write Executors)
- `app/executors/chef.py`, `customer.py`, `driver.py`, `master.py` (ALL 21 EXECUTORS IMPLEMENTED)
- `tests/conftest.py`, `test_chef_executors.py`, `test_customer_executors.py`, `test_driver_executors.py`, `test_master_executors.py` (FULL TEST SUITE)
- `docker-compose.yml`, `scripts/create_db.py`, `.env` (POSTGRESQL SETUP)
- `docs/history.txt` (this Section 30)

NEXT STEP: Implement the 40 LLM Tools (`app/tools/`) wrapping these 21 executors with Guard 2 Pre-Condition Assertions.

















